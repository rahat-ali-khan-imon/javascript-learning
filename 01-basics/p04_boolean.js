// Boolean primitive values
let b1 = true;
let b2 = false;     // direct boolean value (primitive)

console.log(b1, b2);


// Boolean constructor / conversion
let b3 = Boolean(true);
let b4 = Boolean(false);    // true → boolean true (no change)

console.log(b3, b4);